{% block menu %}
<div class="blog-menu">
    <nav class="navbar navbar-expand-lg navbar-dark bg-dark" data-bs-theme="dark">
        <div class="container w-100">
            <div class="row align-items-center w-100">
                <div class="col-12 col-lg-4">
                    <div class="row">
                        <div class="col-6 col-lg-12 d-flex align-items-center">
                            <div class="logo text-white">
                                <a href="{{ baseurl }}">Steven Oyer</a>
                            </div>
                        </div>
    
                        <div class="col-6 col-lg-12">
                            <div class="d-flex justify-content-end">
                                <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#menuBlog" aria-controls="menuBlog" aria-expanded="false" aria-label="Toggle navigation">
                                    <span class="navbar-toggler-icon"></span>
                                </button>
                            </div>
                        </div>
                    </div>
                </div>
                <div class="col-12 col-lg-8">
                    <div class="collapse navbar-collapse justify-content-end" id="menuBlog">
                        <ul class="navbar-nav">
                            <li class="nav-item">
                                <a class="nav-link" href="{{ baseurl }}">Accueil</a>
                            </li>
                            <li class="nav-item">
                                <a class="nav-link" href="{{ baseurl }}/articles">Blog</a>
                            </li>
                            <li class="nav-item">
                                <a class="nav-link" href="{{ baseurl }}#contact">Me contacter</a>
                            </li>
                            
                            {% if not session.id %}
                                <li class="nav-item">
                                    <a class="nav-link" href="{{ baseurl }}/auth">Se connecter / S'inscrire</a>
                                </li>
                            {% endif %}

                            {% if session.id %}
                                <li class="nav-item">
                                    <a class="nav-link" href="{{ baseurl }}/profil">Mon profil</a>
                                </li>
                                <li class="nav-item">
                                    <a class="nav-link" href="{{ baseurl }}/logout">Se déconnecter</a>
                                </li>
                            {% endif %}
                        </ul>
                    </div>
                </div>
            </div>
        </div>
    </nav>
</div>
{% endblock %}
